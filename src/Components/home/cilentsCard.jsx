

// eslint-disable-next-line react/prop-types
const CilentsCard = ({ image }) => {
  return (
    <div className="Clients-div">
      <img className="partners-logo" src={image} alt="" />
    </div>
  );
};

export default CilentsCard;
